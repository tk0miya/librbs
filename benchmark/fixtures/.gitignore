# Populated by `rbs collection install` — regeneratable, not tracked.
.gem_rbs_collection/

# Local bundler config from `bundle install` — regeneratable, not tracked.
.bundle/

# Populated by the conference-app setup step in benchmark/README.md —
# not tracked (a snapshot of an external repo).
conference_app_sig/
