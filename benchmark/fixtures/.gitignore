# The benchmark workload (kaigionrails/conference-app) is cloned here by
# the setup steps in ../README.md, never vendored — this repo keeps no
# other project's sources. The clone (its sig/, gem collection cache,
# bundler config, and gemfiles) is entirely untracked.
conference-app/
